//in any app state simple means the data, here the only one integer value will
//decrement or increment thus one state class is enough
class CounterState{
  int countNum;
  CounterState(this.countNum);

}